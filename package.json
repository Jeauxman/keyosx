{
  "name": "keyosx",
  "version": "1.0.0",
  "private": true,
  "description": "KeyOSX — the master living kiosk for every work Joe has built, is building, or is planning, searched and guided by Jack Rabbit.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.mjs",
    "build": "echo \"KeyOSX is a plain Express app — no build step needed.\"",
    "dev": "node --watch server.mjs",
    "seed": "node scripts/seed.mjs",
    "test": "node --test test/*.test.mjs",
    "smoke-test": "node scripts/smoke-test.mjs",
    "check": "node --check server.mjs && node --check scripts/seed.mjs"
  },
  "dependencies": {
    "express": "^4.21.2",
    "mysql2": "^3.24.4"
  }
}
